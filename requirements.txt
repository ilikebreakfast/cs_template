# =============================================================================
# Top-level requirements — installs all three tiers
# To install selectively, use: make install-core / make install-ml / make install-extras
# To pin versions, run: make pin-deps
# =============================================================================
-r requirements/core.txt
-r requirements/ml.txt
-r requirements/extras.txt
